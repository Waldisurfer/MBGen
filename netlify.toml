[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — all routes served by index.html (required for Vue Router history mode)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
